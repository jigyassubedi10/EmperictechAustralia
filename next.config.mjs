/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true, // This is required for static hosting like Hostinger
  },
};

export default nextConfig;